# Include production dependencies
-r backend/requirements.txt

# Essential testing - Start simple
pytest==8.3.4
pytest-asyncio==0.24.0

# Essential formatting - These never fail, just fix
black==24.10.0
isort==5.13.2
autoflake==2.3.1

# Basic linting - Run manually when ready
flake8==7.1.1
flake8-pyproject==1.2.3  # Allows flake8 config in pyproject.toml

# Type checking
mypy==1.11.2              # For type checking

# Optional: Add these back gradually as needed
# pytest-cov>=4.0.0        # For coverage reports
# pytest-xdist>=3.0.0      # For parallel test execution
# pre-commit>=3.0.0        # For git hooks
